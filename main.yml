    - name: "Installing Apache Webserver"
      hosts: localhost

    - name: "adding php support"
      hosts: localhost

    - name: "creating httpd.conf from httpd.conf.tmpl file"
      hosts: localhost
 
    - name: "creating virtualhost from virtualhost.conf.tmpl file"
      hosts: localhost
 
    - name: "creating documentroot for virtualhost"
      hosts: localhost

    - name: "Restarting/Enabling Apache Webserver"
      hosts: localhost

    - name: "installing mysql server"
      hosts: localhost

    - name: "reset mysql root password"
      hosts: localhost

    - name: "reating my.cnf from template file"
      hosts: localhost

    - name: "restartting mysql server"
      hosts: localhost

    - name: "creating Additional user account"
      hosts: localhost

        #    - name: "updating additional user password"
      #      hosts: localhost
